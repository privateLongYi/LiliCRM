<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linxi.mapper.HospitalMapper">

  <resultMap id="BaseResultMap" type="com.linxi.entity.Hospital">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Thu Aug 20 21:32:10 CST 2020.
    -->
    <id column="h_id" jdbcType="INTEGER" property="hId" />
    <result column="h_name" jdbcType="VARCHAR" property="hName" />
  </resultMap>

  <!--获取所有门诊-->
  <select id="queryHospital" resultMap="BaseResultMap">
    SELECT * FROM hospital
  </select>

  <!--查询门诊-->
  <select id="queryHPage" resultMap="BaseResultMap">
    SELECT * FROM hospital limit #{page}, #{limit}
  </select>

  <!--查询门诊总条数-->
  <select id="getTotal" resultType="Integer">
    SELECT COUNT(*) FROM hospital
  </select>

  <!--新增门诊-->
  <insert id="saveHospital">
    INSERT INTO hospital(h_name) VALUES (#{hName})
  </insert>

  <!--根据编号删除门诊-->
  <delete id="delHByHId">
    DELETE FROM hospital WHERE h_id = #{hId}
  </delete>

  <!--根据编号查询门诊-->
  <select id="queryHByHId" resultMap="BaseResultMap">
    SELECT * FROM hospital WHERE h_id = #{hId}
  </select>

  <!--根据编号编辑门诊-->
  <update id="editHByHId">
    UPDATE hospital SET h_name = #{hName} WHERE h_id = #{hId}
  </update>

</mapper>