<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linxi.mapper.RoleMapper">

  <resultMap id="BaseResultMap" type="com.linxi.entity.Role">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Thu Aug 20 21:32:10 CST 2020.
    -->
    <id column="r_id" jdbcType="INTEGER" property="rId" />
    <result column="r_name" jdbcType="VARCHAR" property="rName" />
  </resultMap>

  <!--查询所有角色-->
  <select id="queryRole" resultMap="BaseResultMap">
    SELECT * FROM role
  </select>

  <!--根据角色名查询角色-->
  <select id="queryRByRName" resultMap="BaseResultMap">
    SELECT * FROM role
    <where>
      <if test="rName != null and rName != ''">
        r_name = #{rName}
      </if>
    </where>
    limit #{page}, #{limit}
  </select>

  <!--查询所有角色总条数-->
  <select id="getTotalByRName" resultType="Integer">
    SELECT COUNT(*) FROM role
    <where>
      <if test="rName != null and rName != ''">
        r_name = #{rName}
      </if>
    </where>
  </select>

  <!--新增角色-->
  <insert id="saveRole">
    INSERT INTO role(r_name) VALUES (#{rName})
  </insert>

  <!--根据编号删除角色-->
  <delete id="delRByRId">
    DELETE FROM role WHERE r_id = #{rId}
  </delete>

  <!--根据编号查询角色-->
  <select id="queryRByRId" resultMap="BaseResultMap">
    SELECT * FROM role WHERE r_id = #{rId}
  </select>

  <!--根据编号编辑角色-->
  <update id="editRByRId">
    UPDATE role SET r_name = #{rName} WHERE r_id = #{rId}
  </update>

</mapper>