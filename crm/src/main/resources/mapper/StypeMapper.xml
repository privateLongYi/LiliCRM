<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linxi.mapper.StypeMapper">

  <resultMap id="BaseResultMap" type="com.linxi.entity.Stype">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Thu Aug 20 21:32:10 CST 2020.
    -->
    <id column="st_id" jdbcType="INTEGER" property="stId" />
    <result column="st_type" jdbcType="VARCHAR" property="stType" />
  </resultMap>

  <!--查询成交类型-->
  <select id="queryStypePage" resultMap="BaseResultMap">
    SELECT * FROM stype limit #{page}, #{limit}
  </select>

  <!--查询成交类型总条数-->
  <select id="getTotal" resultType="Integer">
    SELECT COUNT(*) FROM stype
  </select>

  <!--新增成交类型-->
  <insert id="saveStype">
    INSERT INTO stype(st_type) VALUES (#{stType})
  </insert>

  <!--根据编号删除成交类型-->
  <delete id="delStypeByStId">
    DELETE FROM stype WHERE st_id = #{stId}
  </delete>

  <!--根据编号查询成交类型-->
  <select id="queryStypeByStId" resultMap="BaseResultMap">
    SELECT * FROM stype WHERE st_id = #{stId}
  </select>

  <!--根据编号编辑成交类型-->
  <update id="editStypeByStId">
    UPDATE stype SET st_type = #{stType} WHERE st_id = #{stId}
  </update>

</mapper>