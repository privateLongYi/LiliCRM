<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linxi.mapper.RoleMenuMapper">

  <resultMap id="BaseResultMap" type="com.linxi.entity.RoleMenu">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Thu Aug 20 21:32:10 CST 2020.
    -->
    <id column="rm_id" jdbcType="INTEGER" property="rmId" />
    <result column="rm_r_id" jdbcType="INTEGER" property="rmRId" />
    <result column="rm_m_id" jdbcType="INTEGER" property="rmMId" />
  </resultMap>

  <!--根据角色编号查询权限-->
  <select id="queryRMByRId" resultMap="BaseResultMap">
    SELECT rm.*, m.m_name AS mName, m.m_url AS mUrl, m.m_parent_id AS mParentId,
    m.m_navgition AS mNavgition, (SELECT COUNT(*) FROM menu me WHERE me.m_parent_id = rm.rm_m_id) AS childCount
    FROM role_menu rm INNER JOIN menu m ON rm.rm_m_id=m.m_id
    WHERE rm.rm_r_id = #{rId}
  </select>

  <!--根据角色编号删除角色菜单-->
  <delete id="delRMByRId">
    DELETE FROM role_menu WHERE rm_r_id = #{rId}
  </delete>

  <!--新增角色菜单-->
  <insert id="saveRoleMenu">
    INSERT INTO role_menu(rm_r_id, rm_m_id) VALUES (#{rId}, #{mId})
  </insert>

</mapper>