<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linxi.mapper.RerouteMapper">

  <resultMap id="BaseResultMap" type="com.linxi.entity.Reroute">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Thu Aug 20 21:32:10 CST 2020.
    -->
    <id column="re_id" jdbcType="INTEGER" property="reId" />
    <result column="re_cl_id" jdbcType="INTEGER" property="reClId" />
    <result column="re_h_id" jdbcType="INTEGER" property="reHId" />
    <result column="re_last_time" jdbcType="TIMESTAMP" property="reLastTime" />
    <result column="re_time" jdbcType="TIMESTAMP" property="reTime" />
    <result column="re_cause" jdbcType="LONGVARCHAR" property="reCause" />
    <result column="re_u_id" jdbcType="INTEGER" property="reUId" />
  </resultMap>

  <!--新增改约纪录-->
  <insert id="saveReroute">
    INSERT INTO reroute(re_cl_id, re_h_id, re_last_time, re_time, re_cause, re_u_id)
    VALUES (#{reClId}, #{reHId}, #{reLastTime}, #{reTime}, #{reCause}, #{reUId})
  </insert>

  <!--根据客户名称查询改约客户-->
  <select id="queryRByCName" resultMap="BaseResultMap">
    SELECT r.*, c.c_id AS cId, c.c_name AS cName, h.h_id AS hId,
    h.h_name AS hName FROM reroute r
    LEFT JOIN clue cl ON cl.cl_id=r.re_cl_id
    LEFT JOIN customer c ON c.c_id=cl.cl_c_id
    LEFT JOIN hospital h ON r.re_h_id=h.h_id
    <where>
      <if test="rName != '管理员' and rName != '管理者'">
        AND cl.cl_u_id = #{uId}
      </if>
      <if test="cName != null and cName != ''">
        AND c.c_name LIKE '%${cName}%'
      </if>
    </where>
    <if test="export == 0">
      limit #{page}, #{limit}
    </if>
  </select>

  <!--根据客户名称查询改约客户总数-->
  <select id="getTotalByCName" resultType="Integer">
    SELECT COUNT(*) FROM reroute r
    LEFT JOIN clue cl ON cl.cl_id=r.re_cl_id
    LEFT JOIN customer c ON c.c_id=cl.cl_c_id
    LEFT JOIN hospital h ON r.re_h_id=h.h_id
    <where>
      <if test="rName != '管理员' and rName != '管理者'">
        AND cl.cl_u_id = #{uId}
      </if>
      <if test="cName != null and cName != ''">
        AND c.c_name = #{cName}
      </if>
    </where>
  </select>

  <!--根据线索编号查询改约-->
  <select id="queryReByReClId" resultMap="BaseResultMap">
    SELECT * FROM reroute WHERE re_cl_id = #{reClId}
  </select>

</mapper>