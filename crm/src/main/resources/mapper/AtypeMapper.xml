<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linxi.mapper.AtypeMapper">

  <resultMap id="BaseResultMap" type="com.linxi.entity.Atype">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Thu Aug 20 21:32:10 CST 2020.
    -->
    <id column="at_id" jdbcType="INTEGER" property="atId" />
    <result column="at_type" jdbcType="VARCHAR" property="atType" />
  </resultMap>

  <!--查询所有预约类型-->
  <select id="queryAtype" resultMap="BaseResultMap">
    SELECT * FROM atype
  </select>

  <!--查询预约类型-->
  <select id="queryAtypePage" resultMap="BaseResultMap">
    SELECT * FROM atype limit #{page}, #{limit}
  </select>

  <!--查询预约类型总条数-->
  <select id="getTotal" resultType="Integer">
    SELECT COUNT(*) FROM atype
  </select>

  <!--新增预约类型-->
  <insert id="saveAtype">
    INSERT INTO atype(at_type) VALUES (#{atType})
  </insert>

  <!--根据编号删除预约类型-->
  <delete id="delAtypeByAtId">
    DELETE FROM atype WHERE at_id = #{atId}
  </delete>

  <!--根据编号查询预约类型-->
  <select id="queryAtypeByAtId" resultMap="BaseResultMap">
    SELECT * FROM atype WHERE at_id = #{atId}
  </select>

  <!--根据编号编辑预约类型-->
  <update id="editAtypeByAtId">
    UPDATE atype SET at_type = #{atType} WHERE at_id = #{atId}
  </update>

</mapper>