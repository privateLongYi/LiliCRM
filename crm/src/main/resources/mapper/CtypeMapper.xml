<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linxi.mapper.CtypeMapper">

  <resultMap id="BaseResultMap" type="com.linxi.entity.Ctype">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Thu Aug 20 21:32:10 CST 2020.
    -->
    <id column="ct_id" jdbcType="INTEGER" property="ctId" />
    <result column="ct_type" jdbcType="VARCHAR" property="ctType" />
  </resultMap>

  <!--查询所有客户状态-->
  <select id="queryCtype" resultMap="BaseResultMap">
    SELECT * FROM ctype
  </select>

  <!--查询客户状态-->
  <select id="queryCtypePage" resultMap="BaseResultMap">
    SELECT * FROM ctype limit #{page}, #{limit}
  </select>

  <!--查询客户状态总条数-->
  <select id="getTotal" resultType="Integer">
    SELECT COUNT(*) FROM ctype
  </select>

  <!--新增客户状态-->
  <insert id="saveCtype">
    INSERT INTO ctype(ct_type) VALUES (#{ctType})
  </insert>

  <!--根据编号删除客户状态-->
  <delete id="delCtypeByCtId">
    DELETE FROM ctype WHERE ct_id = #{ctId}
  </delete>

  <!--根据编号查询客户状态-->
  <select id="queryCtypeByCtId" resultMap="BaseResultMap">
    SELECT * FROM ctype WHERE ct_id = #{ctId}
  </select>

  <!--根据编号编辑客户状态-->
  <update id="editCtypeByCtId">
    UPDATE ctype SET ct_type = #{ctType} WHERE ct_id = #{ctId}
  </update>

  <!--根据客户状态查询编号-->
  <select id="queryCtypeByCtType" resultType="Integer">
    SELECT ct_id FROM ctype WHERE ct_type = #{ctType}
  </select>

</mapper>